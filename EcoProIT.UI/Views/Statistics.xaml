<UserControl
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:viewModel="clr-namespace:EcoProIT.UI.ViewModel"
    x:Class="EcoProIT.UI.Views.Statistics" 
             mc:Ignorable="d" MinHeight="453.225" MinWidth="616">

    <UserControl.DataContext>
        <viewModel:StatisticModel/>
    </UserControl.DataContext>

    <Grid>
        <StackPanel Margin="0,0,0,0" >
            <ComboBox HorizontalAlignment="Left" VerticalAlignment="Top" ItemsSource="{Binding Typeof}" SelectedValue="{Binding SelectedType}"/>
            <ComboBox HorizontalAlignment="Left" VerticalAlignment="Top" ItemsSource="{Binding Indexes}" SelectedValue="{Binding SelectedIndex}"/>
            
            <!-- WPFToolkit Chart has been removed during .NET 10 upgrade -->
            <!-- TODO: Implement with LiveCharts or other modern charting library -->
            <Border Height="400" BorderBrush="Gray" BorderThickness="1" Margin="0,0,0,0">
                <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                    <TextBlock Text="{Binding NameOfDiagram}" FontSize="16" FontWeight="Bold" Margin="0,0,0,10"/>
                    <TextBlock Text="Chart placeholder - needs reimplementation with LiveCharts" 
                               Foreground="Gray"
                               TextWrapping="Wrap"
                               TextAlignment="Center"/>
                    <TextBlock Text="{Binding AxisUnit}" FontSize="12" Foreground="DarkGray" Margin="0,10,0,0"/>
                </StackPanel>
            </Border>
        </StackPanel>
    </Grid>
</UserControl>
